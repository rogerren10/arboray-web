version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arboray-web-v3
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_BRAND=cn
    env_file:
      - .env.local